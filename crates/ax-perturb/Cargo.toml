[package]
name = "ax-perturb"
version = "0.1.0"
edition = "2021"

[dependencies]
ax-ir = { path = "../ax-ir" }
ax-tensor = { path = "../ax-tensor" }
num-bigint = "0.4"
num-rational = "0.4"
num-traits = "0.2"
lasso = "0.7"
