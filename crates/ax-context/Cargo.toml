[package]
name = "ax-context"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
anyhow = { workspace = true }
serde = { workspace = true }
toml = "0.8"

[dev-dependencies]
ax-eval = { path = "../ax-eval" }
ax-ir = { path = "../ax-ir" }
