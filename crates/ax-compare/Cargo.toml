[package]
name = "ax-compare"
version = "0.1.0"
edition = "2021"

[dependencies]
ax-ir = { path = "../ax-ir" }
ax-tensor = { path = "../ax-tensor" }
lasso = "0.7"
num-bigint = "0.4"
num-rational = "0.4"
num-traits = "0.2"
