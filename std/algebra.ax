// Standard algebra rules
// Expand
// expand(expr) distributes multiplication over addition
// Factor
// factor(expr) collects terms (reverse of expand)
// Example usage:
// expand((x + 1)^3) → x^3 + 3x^2 + 3x + 1
// simplify(x^2 + 2*x + 1) → collects and simplifies
