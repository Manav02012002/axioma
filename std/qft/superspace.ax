// N=1 superspace operations.
setup_superspace(1)
let Phi = chiral_superfield(Phi)
let Phi_bar = antichiral_superfield(Phi_bar)
let V = vector_superfield_wz(V)
d_alpha(Phi, 0)
d_bar(Phi_bar, 1)
extract_component(Phi * Phi_bar, [1, 1])
superspace_integrate(Phi_bar * Phi, "full")
